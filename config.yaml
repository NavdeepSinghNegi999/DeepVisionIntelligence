paths:
  root_dir: .
  captions: data/raw/captions.txt
  images: data/raw/Images
  processed: data/processed
  artifacts: artifacts
  weights: artifacts/transformer_weights.h5
  checkpoint: artifacts/checkpoints

data:
  batch_size: 32
  image_size: [299, 299]
  max_samples: 1000

training:
  epochs: 1

model:
  embed_dim: 512
  ff_dim: 512
  num_heads: 6
